# Railway Environment Variables Configuration
# Copy these to your Railway dashboard → Project → Variables

# === EMAIL CONFIGURATION ===
EMAIL_USER=wintahboke@gmail.com
EMAIL_PASS=suoj rfnf lvhp basn
ADMIN_EMAIL=wintahboke@gmail.com

# === DATABASE CONFIGURATION ===
# Railway will auto-populate DATABASE_URL when you add MySQL addon
# DATABASE_URL=mysql://user:password@host:port/database

# === FILE UPLOAD CONFIGURATION ===
# Maximum file size (in bytes) - 10MB
MAX_FILE_SIZE=10485760

# Upload directories
UPLOAD_DIR=uploads
RESUME_DIR=uploads/resumes

# === GITHUB CONFIGURATION (Optional) ===
# Add this for higher API rate limits
# GITHUB_TOKEN=your_github_personal_access_token

# === CORS CONFIGURATION ===
VERCEL_URL=https://anita-boke-portfolio.vercel.app
FRONTEND_URL=https://anita-boke-portfolio.vercel.app

# === How to set these in Railway ===
# 1. Go to railway.app
# 2. Open your project: my-portfolio-production-2f89
# 3. Click "Variables" tab
# 4. Add each variable above (except DATABASE_URL - that's auto-generated)
# 5. Click "Deploy" to apply changes